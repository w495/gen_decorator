%% -*- erlang -*-
{erl_opts, [
    debug_info,
    warnings_as_errors
]}.
{cover_enabled, true}.


{deps_dir, "../../../"}.

%%
%% Для внещних хранилищь используем deps
%%
{deps, [
    gen_decorator
]}.